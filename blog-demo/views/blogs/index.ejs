<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .featured-image {
            max-width: 100%;
            height: auto;
        }
        .blog-item {
            margin-bottom: 1.5rem;
        }
        .blog-categories span {
            display: inline-block;
            margin-right: 0.5rem;
            background-color: #f8f9fa;
            padding: 0.2rem 0.5rem;
            border-radius: 0.25rem;
        }
    </style>
</head>
<body>

    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">Blog Manager</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/blogs">Manage Blogs</a></li>
                    <li class="nav-item"><a class="nav-link" href="/categories">Manage Categories</a></li>
                    <li class="nav-item"><a class="nav-link" href="/blogs/new">Create New Blog</a></li>
                    <li class="nav-item"><a class="nav-link" href="/categories/new">Create New Category</a></li>
                    <li class="nav-item"><a class="nav-link" href="/vehicles">List Vehicles</a></li> <!-- Added this line -->
                </ul>
            </div>
            
        </nav>
    </header>

    <div class="container mt-5">
        <h1 class="mb-4">All Blogs</h1>
        <a href="/blogs/new" class="btn btn-primary mb-4">Create New Blog</a>
        <div class="row">
            <% blogs.forEach(blog => { %>
                <div class="col-md-4 blog-item">
                    <div class="card">
                        <% if (blog.featuredImage) { %>
                            <img src="/uploads/<%= blog.featuredImage %>" alt="<%= blog.title %>" class="card-img-top featured-image">
                        <% } %>
                        <div class="card-body">
                            <h5 class="card-title"><a href="/blogs/<%= blog._id %>"><%= blog.title %></a></h5>
                            <p class="blog-categories">Categories: 
                                <% blog.categories.forEach(cat => { %>
                                    <span><%= cat.name %></span>
                                <% }) %>
                            </p>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
